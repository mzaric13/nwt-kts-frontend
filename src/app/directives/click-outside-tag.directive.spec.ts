import { ClickOutsideTagDirective } from './click-outside-tag.directive';

describe('ClickOutsideTagDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickOutsideTagDirective();
    expect(directive).toBeTruthy();
  });
});
