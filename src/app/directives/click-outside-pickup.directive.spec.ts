import { ClickOutsidePickupDirective } from './click-outside-pickup.directive';

describe('ClickOutsidePickupDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickOutsidePickupDirective();
    expect(directive).toBeTruthy();
  });
});
