import { TagFilterUsedPipe } from './tag-filter-used.pipe';

describe('TagFilterUsedPipe', () => {
  it('create an instance', () => {
    const pipe = new TagFilterUsedPipe();
    expect(pipe).toBeTruthy();
  });
});
